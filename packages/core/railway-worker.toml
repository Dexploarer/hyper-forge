# Railway Worker Service Configuration
# Deploys background job processing workers independently from API service
# Uses Railpack (Nixpacks is deprecated) with railpack.json for build config

[build]
builder = "RAILPACK"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
