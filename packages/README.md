# Shared Packages

This directory will contain shared libraries and utilities used across multiple apps in the monorepo.

Future packages:
- `@asset-forge/shared` - Common types, utilities, and constants
- `@asset-forge/ui` - Shared UI components (if needed)

